DB_NAME = 'DB_NAME'
DB_USER = 'DB_USER'
DB_PASS = 'DB_USER'
DB_HOST = 'localhost' # default for any local SQL server
DB_PORT = 5432 # default postgres port


#Get free API key from here https://www.alphavantage.co/support/#api-key
#### NOTE : Free stock API service covering the majority of stock's datasets for up to 25 requests per day #### 
alphavantage_API_KEY =  'API_KEY_HERE' 

sender_email = "SENDERS_EMAIL_HERE"
receiver_email  = "RECEIVERS_EMAIL_HERE"

# To enable app password in gmail, first enable 2FA, then create app password (16 CHARACTERS LONG PASSWORD) 
email_app_passwd= "EMAIL_APP_PASSWORD_HERE" 
